---
apiVersion: v1
kind: Service
metadata:
  name: http
  labels:
    app: http
  annotations:
    checkov.io/skip1: CKV_K8S_21
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
  selector:
    app: http
  type: NodePort # Ignored. Set in Kubernetes Runtime Parameters.
