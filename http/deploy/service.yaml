apiVersion: v1
kind: Service
metadata:
  name: bootz-http
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 80
  - name: grpc
    port: 4002
    protocol: TCP
    targetPort: 4002
  selector:
    app: bootz-http

